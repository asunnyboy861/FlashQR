# FlashQR — Smart Brightness for Easy Scanning

**APP名称**: FlashQR  
**副标题**: Smart Brightness for Easy Scanning  
**目标市场**: 美国 🇺🇸  
**平台**: iOS (iPhone / Apple Watch)  
**文档日期**: 2026-05-07  
**项目类型**: 极简工具类APP

---

## APP命名深度分析

### 命名决策: FlashQR

| 维度 | 分析 | 评分 |
|------|------|------|
| **ASO搜索优化** | "QR"是App Store最高搜索量关键词之一；"Flash"暗示速度+亮度，覆盖"brightness"/"scan"相关搜索意图 | 9/10 |
| **品牌记忆度** | "Flash"双关：闪光(亮度) + 极速(体验)；2音节，发音干脆有力；拼写简单无歧义 | 10/10 |
| **功能描述性** | Flash=自动调亮，QR=二维码展示；用户看到名字即知功能 | 9/10 |
| **情感共鸣** | "Flash"传递即时、轻松、无摩擦的体验感；解决"手忙脚乱调亮度"的焦虑 | 8/10 |
| **国际化适配** | "Flash"和"QR"为全球通用词汇；无需翻译，无文化歧义 | 10/10 |

### 备选命名对比

| 名称 | ASO | 品牌记忆 | 功能描述 | 情感 | 国际化 | 总分 |
|------|-----|----------|----------|------|--------|------|
| **FlashQR** | 9 | 10 | 9 | 8 | 10 | **46** |
| QRBeam | 8 | 7 | 8 | 7 | 9 | 39 |
| GlowQR | 7 | 7 | 7 | 6 | 8 | 35 |
| BrightScan | 7 | 6 | 8 | 6 | 8 | 35 |
| ScanBright | 7 | 5 | 8 | 5 | 8 | 33 |

### ASO关键词策略

**主关键词**: QR code, QR scanner, brightness, barcode, ticket, loyalty card  
**长尾关键词**: QR code brightness, scan QR bright screen, show QR code bright, ticket scanner auto brightness, loyalty card display  
**副标题ASO**: "Smart Brightness for Easy Scanning" — 覆盖 "smart" + "brightness" + "scanning" 搜索意图

---

## 一、痛点深度研究报告

### 1.1 核心痛点矩阵

| 痛点编号 | 痛点描述 | 痛点强度 | 用户频率 | 付费意愿 |
|----------|----------|----------|----------|----------|
| P1 | 屏幕太暗，QR码无法被扫描器识别 | 🔴 极高 | 每次扫码都可能遇到 | 高 |
| P2 | 手动调亮度后忘记恢复，刺眼或费电 | 🔴 极高 | 几乎每次手动调后 | 高 |
| P3 | 部分APP强制100%亮度，暗处刺眼 | 🟡 高 | 使用特定APP时 | 中 |
| P4 | iOS亮度恢复Bug：亮度卡在最低值 | 🔴 极高 | 偶发但影响严重 | 极高 |
| P5 | 相册中的QR码图片无法直接高亮显示 | 🟡 高 | 日常使用 | 高 |
| P6 | 没有统一的QR码/条码展示入口 | 🟠 中 | 日常使用 | 中 |

### 1.2 用户原话（多语言采集）

**英语用户（Reddit r/iphone）**:
> "What I want is to have a QR code in my pictures and when I show it at the shop to have an app that can increase the screen brightness just for a couple of minutes and then go back to auto like nothing happened?"  
> — Reddit r/iphone 用户

> "I know I can adjust the brightness in the control centre but I don't want to mess with that if possible as I like it just the way it's set up!"  
> — Reddit r/iphone 用户

**Apple Community 用户**:
> "I have several apps - McDonald's, KFC, Subway amongst them - that as soon as you display a QR Code for scanning it changes screen brightness to 100%. This is totally annoying if you happen to display the QR Code in a dimly lit room."  
> — Apple Community, 44人点击"Me too"

> "The brightness doesn't often return back to my set brightness - but to the bare minimum to the point I can't use my phone in sunlight or bright environments at all."  
> — Apple Community 用户

**GitHub Issue（corona-warn-app）**:
> "Please implement this. DB Navigator also provides this feature for scanning tickets, and it is very helpful."  
> — GitHub Issue #571

**Apple Community（亮度Bug）**:
> "Yesterday morning on my bed when I am checking some thing in my wallet in my iPhone it suddenly touched boarding pass and my phone brightness automatically increased to 100%. It made my eyes itchy and burn feeling until next 7 hours."  
> — Apple Community 用户

### 1.3 竞品深度分析

| 竞品 | 评分 | 价格 | 亮度功能 | 核心缺陷 |
|------|------|------|----------|----------|
| iOS Wallet | 4.7 | 免费 | 自动调亮但只支持官方PassKit格式 | 不支持自定义图片/相册QR码；亮度恢复Bug |
| Barcodes | 4.8 | $14.99终身 | "Light up your screen" | 3张卡免费限制；功能臃肿；亮度恢复不可靠 |
| KeepCard | 4.8 | 免费+内购 | "Brighter, larger barcode" | 亮度控制粗糙；广告多 |
| Store Cards | 4.6 | 免费+内购 | 增强条码显示 | UI过时；学习曲线陡 |
| Pass2U | 4.5 | 免费+内购 | 支持多种条码格式 | 功能过于复杂；隐私顾虑 |
| QR Code Scanner类 | 3.5-4.4 | 订阅制 | 无亮度功能 | 订阅陷阱；无QR码展示功能 |

### 1.4 竞争空白（我们的机会）

| 空白领域 | 现状 | FlashQR解决方案 |
|----------|------|-----------------|
| 相册QR码智能展示 | 无APP专门做 | 从相册选择→自动高亮→定时恢复 |
| EDR局部高亮技术 | 仅1个开源Demo，无商业APP | Metal+EDR只亮QR码区域，不改变系统亮度 |
| 亮度恢复可靠性 | iOS存在已知Bug | 双重保障：定时恢复+应用生命周期恢复 |
| 极简QR码展示 | 现有APP功能臃肿 | 一键展示，零学习成本 |

---

## 二、核心技术实现

### 2.1 技术架构总览

```
FlashQR App
├── UI Layer (SwiftUI)
│   ├── MainView — 卡片列表/快速入口
│   ├── QRDisplayView — 全屏QR码展示（核心）
│   ├── PhotoPickerView — 相册选择
│   ├── CameraScanView — 相机扫描导入
│   └── SettingsView — 设置
├── Core Layer
│   ├── BrightnessManager — 亮度控制引擎
│   ├── EDRRenderer — Metal+EDR局部高亮渲染
│   ├── QRCodeGenerator — QR码生成（CoreImage）
│   └── AutoRestoreTimer — 定时恢复引擎
├── Data Layer
│   ├── CardStore — 卡片数据管理（SwiftData）
│   ├── PhotoManager — 相册图片管理
│   └── WidgetManager — 小组件数据
└── Extension Layer
    ├── Widget — 锁屏/桌面小组件
    └── Apple Watch — 手表端快速展示
```

### 2.2 核心技术一：EDR局部高亮（差异化杀手锏）

**原理**: 利用iOS 16+的Metal EDR（Extended Dynamic Range）技术，仅将QR码区域亮度提升至超SDR水平，屏幕其他区域保持用户原始亮度设置。**不需要改变系统亮度，不存在恢复Bug**。

**参考项目**: [mszpro/QR-Code-EDR](https://github.com/mszpro/QR-Code-EDR) (MIT License)

#### 核心代码示例 — EDR渲染器

```swift
import Metal
import MetalKit
import CoreImage

class EDRRenderer: NSObject, MTKViewDelegate, ObservableObject {
    
    let imageProvider: (_ scaleFactor: CGFloat, _ headroom: CGFloat) -> CIImage?
    let device: MTLDevice? = MTLCreateSystemDefaultDevice()
    let commandQueue: MTLCommandQueue?
    let renderContext: CIContext?
    let renderQueue = DispatchSemaphore(value: 3)
    
    override init() {
        self.imageProvider = { _, _ in nil }
        self.commandQueue = self.device?.makeCommandQueue()
        if let commandQueue {
            self.renderContext = CIContext(
                mtlCommandQueue: commandQueue,
                options: [
                    .name: "FlashQR_EDRRenderer",
                    .cacheIntermediates: true,
                    .allowLowPower: true
                ]
            )
        } else {
            self.renderContext = nil
        }
        super.init()
    }
    
    init(imageProvider: @escaping (_ scaleFactor: CGFloat, _ headroom: CGFloat) -> CIImage?) {
        self.imageProvider = imageProvider
        self.commandQueue = self.device?.makeCommandQueue()
        if let commandQueue {
            self.renderContext = CIContext(
                mtlCommandQueue: commandQueue,
                options: [
                    .name: "FlashQR_EDRRenderer",
                    .cacheIntermediates: true,
                    .allowLowPower: true
                ]
            )
        } else {
            self.renderContext = nil
        }
        super.init()
    }
    
    func draw(in view: MTKView) {
        guard let commandQueue else { return }
        _ = renderQueue.wait(timeout: .distantFuture)
        
        guard let commandBuffer = commandQueue.makeCommandBuffer(),
              let drawable = view.currentDrawable else {
            renderQueue.signal()
            return
        }
        
        commandBuffer.addCompletedHandler { _ in
            self.renderQueue.signal()
        }
        
        let drawSize = view.drawableSize
        let contentScaleFactor = view.contentScaleFactor
        
        let destination = CIRenderDestination(
            width: Int(drawSize.width),
            height: Int(drawSize.height),
            pixelFormat: view.colorPixelFormat,
            commandBuffer: commandBuffer,
            mtlTextureProvider: { drawable.texture }
        )
        
        var headroom: CGFloat = 1.0
        if #available(iOS 16.0, *) {
            headroom = view.window?.screen.currentEDRHeadroom ?? 1.0
        }
        
        guard var image = self.imageProvider(contentScaleFactor, headroom) else {
            renderQueue.signal()
            return
        }
        
        let iRect = image.extent
        let backBounds = CGRect(x: 0, y: 0, width: drawSize.width, height: drawSize.height)
        let shiftX = round((backBounds.size.width + iRect.origin.x - iRect.size.width) * 0.5)
        let shiftY = round((backBounds.size.height + iRect.origin.y - iRect.size.height) * 0.5)
        image = image.transformed(by: CGAffineTransform(translationX: shiftX, y: shiftY))
        image = image.composited(over: .black)
        
        guard let renderContext else { return }
        _ = try? renderContext.startTask(
            toRender: image,
            from: backBounds,
            to: destination,
            at: CGPoint.zero
        )
        
        commandBuffer.present(drawable)
        commandBuffer.commit()
    }
    
    func mtkView(_ view: MTKView, drawableSizeWillChange size: CGSize) {}
}
```

#### 核心代码示例 — EDR QR码视图

```swift
import SwiftUI
import CoreImage.CIFilterBuiltins

struct EDRQRCodeView: View {
    var qrCodeTextContent: String
    var imageRenderSize: CGSize
    var qrCodeScaleFactor: CGFloat = 15
    
    var body: some View {
        let renderer = EDRRenderer(imageProvider: { scaleFactor, headroom in
            guard let qrFilter = CIFilter(name: "CIQRCodeGenerator") else { return nil }
            let inputData = qrCodeTextContent.data(using: .utf8)
            qrFilter.setValue(inputData, forKey: "inputMessage")
            qrFilter.setValue("H", forKey: "inputCorrectionLevel")
            
            guard let image = qrFilter.outputImage else { return nil }
            let sizeTransform = CGAffineTransform(
                scaleX: qrCodeScaleFactor,
                y: qrCodeScaleFactor
            )
            let qrImage = image.transformed(by: sizeTransform)
            
            let maxRGB = headroom
            guard let EDRColorSpace = CGColorSpace(name: CGColorSpace.extendedLinearSRGB),
                  let maxFillColor = CIColor(
                    red: maxRGB, green: maxRGB, blue: maxRGB,
                    colorSpace: EDRColorSpace
                  ) else { return nil }
            
            let fillImage = CIImage(color: maxFillColor)
            let maskFilter = CIFilter.blendWithMask()
            maskFilter.maskImage = qrImage
            maskFilter.inputImage = fillImage
            
            guard let combinedImage = maskFilter.outputImage else { return nil }
            return combinedImage.cropped(to: CGRect(
                x: 0, y: 0,
                width: imageRenderSize.width * scaleFactor,
                height: imageRenderSize.height * scaleFactor
            ))
        })
        
        EDRMetalView(renderer: renderer)
    }
}
```

#### 核心代码示例 — Metal视图容器

```swift
import SwiftUI
import MetalKit

struct EDRMetalView: UIViewRepresentable {
    @StateObject var renderer: EDRRenderer
    
    func makeUIView(context: Context) -> MTKView {
        let view = MTKView(frame: .zero, device: renderer.device)
        view.preferredFramesPerSecond = 10
        view.framebufferOnly = false
        view.delegate = renderer
        
        if let layer = view.layer as? CAMetalLayer {
            if #available(iOS 16.0, *) {
                layer.wantsExtendedDynamicRangeContent = true
            }
            layer.colorspace = CGColorSpace(name: CGColorSpace.extendedLinearDisplayP3)
            view.colorPixelFormat = MTLPixelFormat.rgba16Float
        }
        return view
    }
    
    func updateUIView(_ view: MTKView, context: Context) {
        view.delegate = renderer
    }
}
```

### 2.3 核心技术二：智能亮度管理器（兼容方案）

**原理**: 对于不支持EDR的设备（iOS 15及以下），使用`UIScreen.main.brightness` API实现传统亮度调节，配合多重恢复保障机制。

#### 核心代码示例 — 亮度管理器

```swift
import SwiftUI

class BrightnessManager: ObservableObject {
    
    static let shared = BrightnessManager()
    
    @Published var isDisplaying = false
    private var originalBrightness: CGFloat = 0
    private var restoreTimer: Timer?
    private var autoRestoreInterval: TimeInterval = 120
    
    private init() {}
    
    func prepareForDisplay() {
        originalBrightness = UIScreen.main.brightness
        isDisplaying = true
    }
    
    func activateFullBrightness() {
        originalBrightness = UIScreen.main.brightness
        UIScreen.main.brightness = 1.0
        isDisplaying = true
        startAutoRestoreTimer()
    }
    
    func restoreBrightness() {
        UIScreen.main.brightness = originalBrightness
        isDisplaying = false
        invalidateTimer()
    }
    
    private func startAutoRestoreTimer() {
        invalidateTimer()
        restoreTimer = Timer.scheduledTimer(
            withTimeInterval: autoRestoreInterval,
            repeats: false
        ) { [weak self] _ in
            self?.restoreBrightness()
        }
    }
    
    func updateAutoRestoreInterval(_ interval: TimeInterval) {
        autoRestoreInterval = interval
        if isDisplaying {
            startAutoRestoreTimer()
        }
    }
    
    private func invalidateTimer() {
        restoreTimer?.invalidate()
        restoreTimer = nil
    }
    
    func handleAppDidEnterBackground() {
        if isDisplaying {
            restoreBrightness()
        }
    }
    
    func handleAppWillEnterForeground() {
        // 不自动恢复亮度，让用户重新触发
    }
    
    deinit {
        invalidateTimer()
    }
}
```

### 2.4 核心技术三：QR码图片识别与展示

#### 核心代码示例 — 从相册图片提取QR码

```swift
import SwiftUI
import PhotosUI
import Vision

struct PhotoQRImporter {
    
    func detectQRCode(from image: UIImage) -> String? {
        guard let cgImage = image.cgImage else { return nil }
        
        let request = VNDetectBarcodesRequest { request, error in
            // Handled synchronously below
        }
        request.symbologies = [.qr, .aztec, .pdf417, .code128]
        
        let handler = VNImageRequestHandler(cgImage: cgImage, options: [:])
        try? handler.perform([request])
        
        guard let observations = request.results as? [VNBarcodeObservation],
              let firstObservation = observations.first,
              let payloadString = firstObservation.payloadStringValue else {
            return nil
        }
        
        return payloadString
    }
    
    func isQRCodeImage(_ image: UIImage) -> Bool {
        return detectQRCode(from: image) != nil
    }
}
```

### 2.5 核心技术四：QR码生成（使用dagronf/QRCode库）

**参考项目**: [dagronf/QRCode](https://github.com/dagronf/QRCode) (MIT License)

```swift
import QRCode

struct QRCodeGenerator {
    
    static func generate(from text: String, size: CGFloat = 300) -> UIImage? {
        let doc = try? QRCode.Document(utf8String: text)
        doc?.errorCorrection = .high
        doc?.design.backgroundColor(CGColor.white)
        doc?.design.foregroundColor(CGColor.black)
        return try? doc?.uiImage(CGSize(width: size, height: size))
    }
    
    static func generateStyled(
        from text: String,
        size: CGFloat = 300,
        foregroundColor: CGColor = CGColor.black,
        backgroundColor: CGColor = CGColor.white,
        eyeShape: QRCode.EyeShape = .square,
        pixelShape: QRCode.PixelShape = .square
    ) -> UIImage? {
        let doc = try? QRCode.Document(utf8String: text)
        doc?.errorCorrection = .high
        doc?.design.backgroundColor(backgroundColor)
        doc?.design.foregroundColor(foregroundColor)
        doc?.design.style.eye = eyeShape
        doc?.design.style.onPixels.shape = pixelShape
        return try? doc?.uiImage(CGSize(width: size, height: size))
    }
}
```

### 2.6 编写规则

| 规则 | 说明 |
|------|------|
| 最低支持版本 | iOS 16.0（EDR功能需要） |
| UI框架 | SwiftUI only，不使用UIKit（除MTKView封装） |
| 数据存储 | SwiftData（iOS 17+），UserDefaults用于设置 |
| 并发模型 | async/await + Actor，不使用Combine |
| 亮度恢复 | 三重保障：定时器 + 生命周期 + 手动按钮 |
| EDR降级 | 不支持EDR的设备自动降级为传统亮度方案 |
| 隐私原则 | 所有数据本地存储，无网络请求，无追踪 |
| 无障碍 | VoiceOver完整支持，Dynamic Type |

---

## 三、实现流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                     FlashQR 实现流程图                           │
└─────────────────────────────────────────────────────────────────┘

Phase 1: 项目初始化 (Day 1)
    │
    ├── 创建Xcode项目 (SwiftUI App, iOS 16+)
    ├── 配置SPM依赖
    │   ├── dagronf/QRCode (QR码生成)
    │   └── 无其他第三方依赖
    ├── 配置Info.plist
    │   ├── NSPhotoLibraryUsageDescription
    │   └── NSCameraUsageDescription
    └── 创建项目目录结构
        ├── Models/
        ├── Views/
        ├── ViewModels/
        ├── Services/
        └── Extensions/

Phase 2: 核心功能开发 (Day 2-4)
    │
    ├── BrightnessManager 开发
    │   ├── 保存/恢复原始亮度
    │   ├── 定时自动恢复
    │   └── 应用生命周期处理
    │
    ├── EDR渲染引擎开发
    │   ├── EDRRenderer (Metal渲染)
    │   ├── EDRMetalView (SwiftUI封装)
    │   └── EDR降级策略
    │
    ├── QR码展示视图开发
    │   ├── EDR模式 (iOS 16+, Metal)
    │   ├── 传统模式 (亮度调节)
    │   └── 自动模式选择
    │
    └── 相册导入功能
        ├── PhotosPicker集成
        ├── Vision QR码检测
        └── 图片缓存管理

Phase 3: 卡片管理开发 (Day 5-6)
    │
    ├── SwiftData数据模型
    │   ├── QRCard (id, name, content, type, imageData, createdAt)
    │   └── AppSettings (autoRestoreInterval, displayMode, theme)
    │
    ├── 卡片CRUD操作
    │   ├── 添加卡片（扫描/相册/手动输入）
    │   ├── 编辑卡片
    │   ├── 删除卡片
    │   └── 排序/搜索
    │
    └── 卡片列表视图
        ├── 卡片网格/列表切换
        ├── 快速展示入口
        └── 滑动操作

Phase 4: 扩展功能开发 (Day 7-8)
    │
    ├── 桌面小组件
    │   ├── WidgetKit配置
    │   ├── 小型卡片快捷入口
    │   └── 中型卡片预览+快捷
    │
    ├── Apple Watch应用
    │   ├── 快速卡片展示
    │   └── 手腕抬升自动亮屏
    │
    └── 设置页面
        ├── 自动恢复时间设置
        ├── 展示模式选择(EDR/传统/自动)
        └── 外观设置

Phase 5: 测试与发布 (Day 9-10)
    │
    ├── 真机测试
    │   ├── EDR功能验证（需真机）
    │   ├── 亮度恢复可靠性测试
    │   └── 各种扫码器兼容测试
    │
    ├── App Store准备
    │   ├── 截图制作
    │   ├── 描述文案
    │   └── 隐私政策
    │
    └── 提交审核
```

---

## 四、用户流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                      FlashQR 用户流程图                          │
└─────────────────────────────────────────────────────────────────┘

场景A: 首次使用
═══════════════
    用户打开APP
        │
        ▼
    ┌──────────────┐
    │  欢迎引导页   │ ← 3页滑动引导
    │  (Onboarding) │    1. "展示QR码，自动调亮"
    └──────┬───────┘    2. "用完自动恢复，无需操心"
           │            3. "从相册导入，一键展示"
           ▼
    ┌──────────────┐
    │  空白主页面   │
    │  + 添加按钮   │
    └──────┬───────┘
           │
           ▼
    ┌──────────────────────┐
    │  选择添加方式         │
    │  ┌────────────────┐  │
    │  │ 📷 扫描QR码    │  │
    │  ├────────────────┤  │
    │  │ 🖼️ 从相册选择  │  │
    │  ├────────────────┤  │
    │  │ ⌨️ 手动输入    │  │
    │  └────────────────┘  │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────┐
    │  QR码已保存       │
    │  卡片出现在列表中  │
    └──────────────────┘


场景B: 日常使用 — 展示QR码（核心流程）
════════════════════════════════════
    用户打开APP
        │
        ▼
    ┌──────────────────┐
    │  卡片列表主页面   │ ← 显示所有保存的QR码卡片
    └──────┬───────────┘
           │
     用户点击某张卡片
           │
           ▼
    ┌──────────────────────────────────┐
    │  QR码全屏展示页面                 │
    │  ┌────────────────────────────┐  │
    │  │                            │  │
    │  │     ┌──────────────┐       │  │
    │  │     │              │       │  │
    │  │     │   QR Code    │       │  │ ← EDR模式：仅QR码区域超亮
    │  │     │  (EDR高亮)   │       │  │   传统模式：全屏100%亮度
    │  │     │              │       │  │
    │  │     └──────────────┘       │  │
    │  │                            │  │
    │  │  卡片名称                   │  │
    │  │                            │  │
    │  │  ┌────────┐  ┌──────────┐  │  │
    │  │  │ 手动恢复 │  │ 延长展示  │  │  │
    │  │  └────────┘  └──────────┘  │  │
    │  └────────────────────────────┘  │
    └──────────────┬───────────────────┘
                   │
         ┌─────────┴──────────┐
         │                    │
    用户关闭页面          定时器到期(2分钟)
         │                    │
         ▼                    ▼
    ┌──────────────┐   ┌──────────────┐
    │  亮度自动恢复  │   │  亮度自动恢复  │
    │  返回卡片列表  │   │  返回卡片列表  │
    └──────────────┘   └──────────────┘


场景C: 从小组件快速展示
══════════════════════
    用户在锁屏/桌面
        │
        ▼
    ┌──────────────────┐
    │  FlashQR小组件    │ ← 显示最常用的1-2张卡片
    │  [CVS] [Starbucks]│
    └──────┬───────────┘
           │
     用户点击卡片
           │
           ▼
    ┌──────────────────┐
    │  APP自动打开       │
    │  直接进入全屏展示   │ ← 跳过列表页，直达展示
    │  QR码高亮显示      │
    └──────────────────┘


场景D: Apple Watch快速展示
══════════════════════════
    用户抬腕
        │
        ▼
    ┌──────────────────┐
    │  Watch APP        │
    │  卡片列表          │
    └──────┬───────────┘
           │
     用户点击卡片
           │
           ▼
    ┌──────────────────┐
    │  Watch全屏展示    │
    │  QR码 + 自动调亮  │
    └──────────────────┘
```

---

## 五、软件数据流图

```
┌─────────────────────────────────────────────────────────────────┐
│                     FlashQR 数据流图                             │
└─────────────────────────────────────────────────────────────────┘

                    ┌─────────────┐
                    │   用户操作   │
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
              ▼            ▼            ▼
        ┌──────────┐ ┌──────────┐ ┌──────────┐
        │ 添加卡片  │ │ 展示QR码  │ │ 管理设置  │
        └────┬─────┘ └────┬─────┘ └────┬─────┘
             │            │            │
             ▼            ▼            ▼
    ┌────────────────┐ ┌────────────────────────────┐ ┌──────────────┐
    │ QR码数据输入    │ │  QR码展示引擎               │ │ UserDefaults │
    │                │ │                            │ │              │
    │ · 相册图片     │ │  ┌─────────────────────┐   │ │ · 恢复时间   │
    │ · 相机扫描     │ │  │ EDR模式(iOS 16+)    │   │ │ · 展示模式   │
    │ · 手动输入     │ │  │                     │   │ │ · 主题       │
    │ · 剪贴板      │ │  │ Metal Renderer      │   │ └──────────────┘
    └───────┬────────┘ │  │ ↓                   │   │
            │          │  │ CIImage + EDR Head  │   │
            ▼          │  │ ↓                   │   │
    ┌────────────────┐ │  │ MTKView 渲染输出    │   │
    │ Vision框架     │ │  └─────────────────────┘   │
    │                │ │           │                 │
    │ · QR码检测     │ │  ┌─────────────────────┐   │
    │ · 条码类型识别  │ │  │ 传统模式(iOS 15)    │   │
    │ · 内容提取     │ │  │                     │   │
    └───────┬────────┘ │  │ UIScreen.brightness │   │
            │          │  │ ↓                   │   │
            ▼          │  │ 100% → 定时恢复      │   │
    ┌────────────────┐ │  └─────────────────────┘   │
    │ SwiftData      │ │           │                 │
    │                │ │           ▼                 │
    │ · QRCard模型   │ │  ┌─────────────────────┐   │
    │ · CRUD操作     │ │  │ BrightnessManager   │   │
    │ · iCloud同步   │ │  │                     │   │
    └───────┬────────┘ │  │ · 保存原始亮度      │   │
            │          │  │ · 定时自动恢复      │   │
            │          │  │ · 生命周期恢复      │   │
            │          │  │ · 手动恢复按钮      │   │
            │          │  └─────────────────────┘   │
            │          └────────────────────────────┘
            │                      │
            ▼                      ▼
    ┌────────────────────────────────────────┐
    │              输出层                     │
    │                                        │
    │  ┌──────────┐  ┌──────────┐           │
    │  │ 屏幕显示  │  │ 小组件   │           │
    │  │ · QR码   │  │ · 快捷卡 │           │
    │  │ · 高亮   │  │ · 预览   │           │
    │  └──────────┘  └──────────┘           │
    │                                        │
    │  ┌──────────┐  ┌──────────┐           │
    │  │ Watch    │  │ 剪贴板   │           │
    │  │ · 快速展 │  │ · 复制   │           │
    │  └──────────┘  └──────────┘           │
    └────────────────────────────────────────┘

数据存储架构:
════════════

    SwiftData (本地)
    ├── QRCard
    │   ├── id: UUID
    │   ├── name: String
    │   ├── content: String (QR码文本内容)
    │   ├── type: QRType (qr, aztec, pdf417, code128, image)
    │   ├── imageData: Data? (原始图片，用于图片型QR码)
    │   ├── colorHex: String? (自定义颜色)
    │   ├── isFavorite: Bool
    │   ├── displayCount: Int (展示次数统计)
    │   ├── lastDisplayedAt: Date?
    │   └── createdAt: Date
    │
    └── AppSettings
        ├── autoRestoreInterval: Double (秒)
        ├── displayMode: DisplayMode (edr, traditional, auto)
        └── theme: AppTheme

    UserDefaults
    ├── hasCompletedOnboarding: Bool
    ├── defaultCardId: UUID? (小组件默认卡片)
    └── appLaunchCount: Int
```

---

## 六、极具竞争力的价格策略

### 6.1 价格策略深度分析

#### 市场价格调研

| 竞品 | 免费功能 | 付费模式 | 价格 | 用户评价 |
|------|----------|----------|------|----------|
| Barcodes | 3张卡片 | 终身买断 | $14.99 | "太贵但好用" |
| KeepCard | 基础功能+广告 | 订阅 | $2.99/月 | "广告烦人" |
| Store Cards | 基础功能 | 订阅 | $3.99/月 | "功能复杂" |
| Pass2U | 基础功能 | 订阅 | $2.99/月 | "订阅陷阱" |
| QR Scanner类 | 几乎无 | 订阅 | $4.99-9.99/周 | "骗子APP" |

#### 价格策略选择分析

| 策略 | 优势 | 劣势 | 适合度 |
|------|------|------|--------|
| 完全免费 | 最大下载量，零摩擦 | 无收入 | ❌ 不可持续 |
| 下载前付费 | 高质量用户，无广告 | 下载量低，ASO差 | ⚠️ 功能太简单不值得付费 |
| 免费+订阅A | 引流效果好，持续收入 | 用户抵触订阅 | ⚠️ 功能简单不适合订阅 |
| **免费+一次性买断** | **低摩擦，高转化，用户友好** | **单次收入** | **✅ 最佳选择** |

### 6.2 最终价格策略：免费下载 + 一次性买断

**策略类型**: 站内订阅A变体 — 针对功能简单软件

**核心理由**:
1. **功能极简**：核心功能就是"展示QR码+自动调亮"，没有API费用，没有持续成本
2. **用户心理**：$0.99-$1.99的买断价格用户几乎不假思索
3. **差异化竞争**：所有竞品要么限制卡片数量，要么订阅制，买断制是巨大优势
4. **ASO优势**：免费下载 → 高下载量 → 高排名 → 更多下载
5. **口碑传播**："这么好用才$1.99，太值了" → 自然传播

### 6.3 详细定价方案

```
┌─────────────────────────────────────────────────────────────────┐
│                    FlashQR 定价方案                               │
└─────────────────────────────────────────────────────────────────┘

免费版（永久免费）
═════════════════
    ✅ 保存 3 张QR码卡片
    ✅ EDR高亮展示（核心功能）
    ✅ 传统亮度调节展示
    ✅ 从相册导入QR码
    ✅ 相机扫描导入
    ✅ 手动输入QR码内容
    ✅ 自动亮度恢复（2分钟）
    ✅ 手动恢复按钮
    ✅ 深色模式
    ✅ 无广告（免费版也无广告！）

Pro版（一次性买断 $1.99）
═════════════════════════
    ✅ 免费版所有功能
    ✅ 无限卡片数量
    ✅ 自定义自动恢复时间（1-10分钟）
    ✅ 桌面小组件（锁屏+桌面）
    ✅ Apple Watch应用
    ✅ iCloud同步
    ✅ 自定义卡片颜色/图标
    ✅ QR码生成器（从文本生成QR码）
    ✅ 剪贴板快速导入
    ✅ 卡片分组/文件夹
```

### 6.4 定价心理学分析

| 定价元素 | 策略 | 原理 |
|----------|------|------|
| 价格锚点 | 先展示$14.99竞品价格，再展示$1.99 | 锚定效应 |
| 免费版价值 | 核心功能免费，无广告 | 互惠原则：用户觉得欠你，更愿意付费 |
| 买断vs订阅 | 强调"一次性买断，永久使用" | 损失厌恶：用户害怕持续付费 |
| 3张卡片限制 | 刚好够日常使用，但不够舒适 | 部分满足：让用户想升级但不愤怒 |
| 价格数字 | $1.99而非$2.99 | 左位数效应：1开头感觉比2便宜很多 |

### 6.5 收入预测

| 指标 | 保守估计 | 中等估计 | 乐观估计 |
|------|----------|----------|----------|
| 月下载量 | 5,000 | 15,000 | 50,000 |
| Pro转化率 | 5% | 8% | 12% |
| 月Pro购买 | 250 | 1,200 | 6,000 |
| 月收入 | $498 | $2,388 | $11,940 |
| 年收入 | $5,970 | $28,650 | $143,280 |

### 6.6 为什么不选订阅制

| 考量 | 分析 |
|------|------|
| 功能复杂度 | 极简工具，无持续开发成本 |
| API费用 | 无（全部本地处理） |
| 服务器费用 | 无（无后端） |
| 用户期望 | 工具类APP用户强烈偏好买断 |
| 竞品差异化 | 竞品几乎都是订阅制，买断是差异化优势 |
| App Store趋势 | Apple越来越要求订阅APP提供持续价值 |

---

## 七、符合美国市场的UI设计

### 7.1 设计原则

| 原则 | 说明 |
|------|------|
| **极简至上** | 美国用户偏好"打开即用"的工具APP，零学习成本 |
| **大而清晰** | QR码展示区域最大化，文字大而清晰 |
| **深色优先** | 深色模式为默认（展示QR码时对比度最高） |
| **手势驱动** | 滑动删除、长按快捷操作、双击放大 |
| **无障碍** | VoiceOver、Dynamic Type、高对比度支持 |

### 7.2 配色方案

```
主色调（Dark Mode默认）:
    背景: #000000 (纯黑，OLED友好)
    卡片背景: #1C1C1E (iOS系统深灰)
    主强调色: #0A84FF (iOS系统蓝)
    QR码背景: #FFFFFF (纯白，最高对比度)
    QR码前景: #000000 (纯黑，最高对比度)

辅助色:
    成功: #30D158 (绿色，已扫描确认)
    警告: #FF9F0A (橙色，定时器即将到期)
    危险: #FF453A (红色，删除确认)

Light Mode:
    背景: #F2F2F7 (iOS系统浅灰)
    卡片背景: #FFFFFF
    主强调色: #007AFF
```

### 7.3 页面设计规格

#### 主页面 — 卡片列表

```
┌─────────────────────────────────┐
│  FlashQR                    [+] │  ← 导航栏：标题 + 添加按钮
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────┐   │
│  │  ┌───┐                  │   │
│  │  │QR │  CVS Pharmacy    │   │  ← 卡片：缩略图 + 名称 + 类型标签
│  │  │   │  Loyalty Card    │   │
│  │  └───┘  Last used: 2d   │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │  ┌───┐                  │   │
│  │  │QR │  Starbucks       │   │
│  │  │   │  Gift Card       │   │
│  │  └───┘  Last used: 5h   │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │  ┌───┐                  │   │
│  │  │QR │  Concert Ticket  │   │
│  │  │   │  Event           │   │
│  │  └───┘  Last used: never│   │
│  └─────────────────────────┘   │
│                                 │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─  │
│  Unlock unlimited cards → $1.99│  ← Pro升级入口（柔和提示）
│                                 │
└─────────────────────────────────┘
```

#### QR码展示页面（核心页面）

```
┌─────────────────────────────────┐
│                                 │  ← 状态栏（隐藏，沉浸式）
│                                 │
│         CVS Pharmacy            │  ← 卡片名称（小字）
│                                 │
│     ┌───────────────────┐       │
│     │                   │       │
│     │                   │       │
│     │     ┌─────────┐   │       │
│     │     │ ███████ │   │       │  ← EDR高亮QR码区域
│     │     │ ██ █ ██ │   │       │    （比周围更亮）
│     │     │ ███████ │   │       │
│     │     │ ██ █ ██ │   │       │
│     │     │ ███████ │   │       │
│     │     └─────────┘   │       │
│     │                   │       │
│     │   ████████████    │       │  ← 白色背景区域
│     │                   │       │
│     └───────────────────┘       │
│                                 │
│     Auto-restore in 1:45        │  ← 倒计时提示
│                                 │
│  ┌──────────┐  ┌──────────────┐│
│  │  ✕ Close  │  │  +2 min More ││  ← 两个操作按钮
│  └──────────┘  └──────────────┘│
│                                 │
└─────────────────────────────────┘
```

#### 添加卡片页面

```
┌─────────────────────────────────┐
│  ← Add Card                     │
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────┐   │
│  │  📷  Scan QR Code       │   │  ← 相机扫描（最大按钮）
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │  🖼️  From Photos        │   │  ← 从相册选择
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │  ⌨️  Enter Manually      │   │  ← 手动输入
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │  📋  From Clipboard     │   │  ← 剪贴板导入（Pro）
│  └─────────────────────────┘   │
│                                 │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─  │
│  Card Name:                     │
│  ┌─────────────────────────┐   │
│  │ CVS Pharmacy            │   │
│  └─────────────────────────┘   │
│                                 │
│           [Save Card]           │
│                                 │
└─────────────────────────────────┘
```

### 7.4 动效设计

| 交互 | 动效 | 时长 |
|------|------|------|
| 点击卡片→展示 | 卡片放大+淡入全屏 | 0.3s spring |
| 展示页面→关闭 | 缩小回卡片位置 | 0.25s ease-out |
| QR码出现 | 从中心扩展+亮度渐亮 | 0.4s ease-in-out |
| 定时器倒计时 | 环形进度条平滑递减 | 持续 |
| 亮度恢复 | 渐变恢复（非突变） | 0.5s ease-out |
| 添加卡片成功 | 卡片从顶部滑入列表 | 0.3s spring |
| 删除卡片 | 滑动+淡出 | 0.25s ease-out |

### 7.5 当前主流UI趋势适配

| 趋势 | FlashQR实现 |
|------|-------------|
| 大标题导航 | 主页使用大标题"FlashQR" |
| 卡片式布局 | 列表项使用圆角卡片 |
| 沉浸式全屏 | QR展示页隐藏导航栏和状态栏 |
| 手势优先 | 滑动删除、下拉关闭 |
| 动态岛适配 | 展示QR码时Live Activity显示倒计时 |
| 控制中心 | 提供Control Center快捷方式（iOS 18+） |

---

## 八、可二次开发的开源项目参考

### 8.1 核心参考项目

| 项目 | 地址 | 许可证 | 用途 | 推荐度 |
|------|------|--------|------|--------|
| **QR-Code-EDR** | [mszpro/QR-Code-EDR](https://github.com/mszpro/QR-Code-EDR) | MIT | EDR局部高亮QR码渲染（杀手锏功能） | ⭐⭐⭐⭐⭐ |
| **QRCode** | [dagronf/QRCode](https://github.com/dagronf/QRCode) | MIT | QR码生成+样式化（SwiftUI原生支持） | ⭐⭐⭐⭐⭐ |
| **QR-Pop** | [git-shawn/QR-Pop](https://github.com/git-shawn/QR-Pop) | 开源 | QR码生成APP完整实现参考（SwiftUI） | ⭐⭐⭐⭐ |
| **QRcoder** | [yep/QRcoder](https://github.com/yep/QRcoder) | MIT | QR码生成器（iOS/macOS） | ⭐⭐⭐ |

### 8.2 技术参考项目

| 技术 | 参考来源 | 说明 |
|------|----------|------|
| UIScreen.brightness | iOS原生API | 传统亮度调节方案 |
| Metal + EDR | [mszpro/QR-Code-EDR](https://github.com/mszpro/QR-Code-EDR) | 局部高亮方案 |
| Vision VNDetectBarcodesRequest | iOS原生API | QR码图片识别 |
| SwiftData | iOS 17+原生框架 | 数据持久化 |
| WidgetKit | iOS原生框架 | 桌面/锁屏小组件 |
| WatchKit | iOS原生框架 | Apple Watch应用 |

### 8.3 二次开发注意事项

| 项目 | 注意事项 |
|------|----------|
| QR-Code-EDR | MIT许可证，可自由使用；需iOS 16+真机测试EDR；模拟器不支持EDR |
| dagronf/QRCode | MIT许可证；SPM集成；支持Objective-C；需配置`OTHER_LDFLAGS: -ObjC` |
| QR-Pop | 参考UI设计和架构模式；不要直接复制代码（未明确许可证） |

---

## 九、竞争差异化总结

### 9.1 FlashQR vs 竞品对比矩阵

| 功能 | FlashQR | Barcodes | KeepCard | iOS Wallet |
|------|---------|----------|----------|------------|
| EDR局部高亮 | ✅ 独有 | ❌ | ❌ | ❌ |
| 自动亮度恢复 | ✅ 三重保障 | ⚠️ 基础 | ⚠️ 基础 | ⚠️ 有Bug |
| 相册QR码导入 | ✅ | ✅ | ✅ | ❌ |
| 极简UI | ✅ | ❌ 臃肿 | ⚠️ 一般 | ✅ |
| 买断制定价 | ✅ $1.99 | ❌ $14.99 | ❌ 订阅 | 免费 |
| 免费版无广告 | ✅ | ❌ | ❌ | ✅ |
| Apple Watch | ✅ | ✅ | ❌ | ✅ |
| 小组件 | ✅ | ✅ | ✅ | ✅ |
| QR码生成 | ✅ | ❌ | ❌ | ❌ |
| 零学习成本 | ✅ | ❌ | ⚠️ | ✅ |

### 9.2 核心竞争优势（USP）

1. **EDR局部高亮技术** — 全球首个商业APP使用Metal EDR仅高亮QR码区域，不改变系统亮度，彻底解决亮度恢复Bug
2. **极简体验** — 打开→点击→展示，3步完成，零学习成本
3. **买断$1.99** — 对比竞品$14.99或月度订阅，价格极具杀伤力
4. **免费版无广告** — 建立信任，提高Pro转化率
5. **三重亮度恢复保障** — 定时器+生命周期+手动按钮，最可靠的亮度恢复

---

## 十、开发时间表

| 阶段 | 天数 | 交付物 |
|------|------|--------|
| Phase 1: 项目初始化 | Day 1 | Xcode项目+SPM配置+目录结构 |
| Phase 2: 核心功能 | Day 2-4 | EDR渲染+亮度管理+QR展示 |
| Phase 3: 卡片管理 | Day 5-6 | SwiftData模型+CRUD+列表UI |
| Phase 4: 扩展功能 | Day 7-8 | 小组件+Watch+设置 |
| Phase 5: 测试发布 | Day 9-10 | 真机测试+截图+提交审核 |
| **总计** | **10天** | **完整可发布APP** |

---

## 附录A：App Store元数据（英文）

**App Name**: FlashQR  
**Subtitle**: Smart Brightness for Easy Scanning  
**Keywords**: QR code, brightness, barcode, scanner, ticket, loyalty card, wallet, display, flash, auto  
**Description**:

```
FlashQR makes showing QR codes effortless. No more fumbling with brightness controls — just tap and show.

SMART BRIGHTNESS
FlashQR automatically brightens your QR code for easy scanning, then restores your original brightness when you're done. No more blinding yourself at night or forgetting to turn brightness back down.

EDR TECHNOLOGY
Using advanced Metal rendering, FlashQR can brighten just the QR code area of your screen — without changing your system brightness. Your eyes stay comfortable while the scanner reads your code perfectly.

WORKS WITH ANY QR CODE
• Import from your photo library
• Scan with your camera  
• Type or paste QR code content
• Generate new QR codes from text

ALWAYS READY
• Home screen widgets for instant access
• Apple Watch app for quick display
• Auto-restore timer so you never get stuck at full brightness

FREE TO USE
Save up to 3 cards for free, with no ads ever. Upgrade to Pro for unlimited cards, widgets, and Apple Watch for just $1.99 — one time, forever.

No subscriptions. No ads. No tracking. Just flash your QR code and go.
```

**Category**: Utilities  
**Age Rating**: 4+  
**Privacy**: No data collected

---

## 附录B：关键技术决策记录

| 决策 | 选择 | 原因 |
|------|------|------|
| UI框架 | SwiftUI | 现代化、声明式、小组件/Watch原生支持 |
| 数据存储 | SwiftData | iOS 17+原生，比Core Data更简洁 |
| QR码生成 | dagronf/QRCode | 功能最全、SwiftUI原生、MIT许可 |
| EDR渲染 | Metal + CIImage | 唯一实现局部高亮的技术方案 |
| 最低版本 | iOS 16 | EDR需要iOS 16+ |
| 小组件 | WidgetKit | 原生框架，无需第三方 |
| 无网络请求 | 全部本地 | 隐私优先，无服务器成本 |
| 无第三方分析 | 不集成 | 隐私优先，App Store审核友好 |
